import styled from "styled-components";

const StyledMyCoursePage = styled.div`
  .cousehub__subscribe-courses {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1rem;
  }
`;

export default StyledMyCoursePage;
